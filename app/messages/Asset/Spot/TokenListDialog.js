import { defineMessages } from 'react-intl';

const prefix = 'Asset.Spot.TokenListDialog';

const definedMessages = defineMessages({
  selectToken: {
    id: `${prefix}.selectToken`,
    description: '选择币种',
    defaultMessage: 'Select a Token',
  },
});

export default definedMessages;
