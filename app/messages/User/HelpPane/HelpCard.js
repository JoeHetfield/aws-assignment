import { defineMessages } from 'react-intl';

const prefix = 'User.HelpPane.HelpCard';

const definedMessages = defineMessages({
  help: {
    id: `${prefix}.help`,
    description: '帮助中心',
    defaultMessage: 'Help Center',
  },
});

export default definedMessages;
