const prefix = 'DRAWER_MENU';

// ////////////////////////////////

const TOGGLE = `${prefix}.TOGGLE`;

const toggle = () => ({
  type: TOGGLE,
});

// ////////////////////////////////

export default {
  // drawer menu
  TOGGLE,
  toggle,
};
